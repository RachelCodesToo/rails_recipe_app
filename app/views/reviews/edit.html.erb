<h1> Edit Review! </h1>

<%= render partial: "layouts/errors", locals: {object: @review} %>

<%= form_for(@review) do |f|%>

<%= f.label :review %>
<%= f.collection_select :recipe_id, Recipe.all, :id, :recipeName %>

<%= f.label :content %>
<%= f.text_field :content %>

<%= f.submit %>
<% end %>